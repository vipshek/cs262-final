Reconcile differences between GameServer interface and ClusterServer interface

(copy SimpleServer to ClusterGameServer)
SimpleServer implements GameServer,ClusterServer

make the CGServer have a UUID and master property
make the CGServer throw notMasterException
make the CGClient CATCH notMasterException

make the CGServer (for now) loop if not the master, heartbeating the master


for Perry/Buff -> make their server replicate state to the slaves


RENAME GAMECLUSTERSERVER to SIMPLECLUSTERGAMESERVER
